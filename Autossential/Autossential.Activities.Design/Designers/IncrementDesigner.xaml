<sap:ActivityDesigner x:Class="Autossential.Activities.Design.Designers.IncrementDesigner"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:activity="clr-namespace:Autossential.Activities.Properties;assembly=Autossential.Activities"
                      xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation"
                      xmlns:sapv="clr-namespace:System.Activities.Presentation.View;assembly=System.Activities.Presentation"
                      xmlns:sapc="clr-namespace:System.Activities.Presentation.Converters;assembly=System.Activities.Presentation"
                      xmlns:converters="clr-namespace:UiPath.Shared.Activities.Design.Converters"
                      xmlns:uip="clr-namespace:UiPath.Shared.Activities.Design.Controls"
                      xmlns:ds="clr-namespace:Autossential.Activities.Design.Properties">

    <sap:ActivityDesigner.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Themes\Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <sapc:ArgumentToExpressionConverter x:Key="ArgumentToExpressionConverter" />
            <converters:ActivityIconConverter x:Key="ActivityIconConverter" />
        </ResourceDictionary>
    </sap:ActivityDesigner.Resources>

    <sap:ActivityDesigner.Icon>
        <DrawingBrush Stretch="Uniform" Drawing="{Binding Path=ModelItem, Converter={StaticResource ActivityIconConverter}, ConverterParameter=pack://application:\,\,\,/Autossential.Activities.Design;component/themes/icons.xaml}" />
    </sap:ActivityDesigner.Icon>

    <uip:ActivityDecoratorControl Style="{StaticResource ActivityDecoratorStyle}">
        <Grid Width="240">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="40" />
            </Grid.ColumnDefinitions>

            <sapv:ExpressionTextBox Grid.Column="0" ExpressionType="{Binding ModelItem.Properties[Variable].PropertyType.GenericTypeArguments[0]}" OwnerActivity="{Binding Path=ModelItem}" HintText="{x:Static activity:Resources.Increment_Variable_Description}" UseLocationExpression="True">
                <sapv:ExpressionTextBox.Expression>
                    <Binding Path="ModelItem.Variable" Converter="{StaticResource ArgumentToExpressionConverter}" Mode="TwoWay" ConverterParameter="InOut" UpdateSourceTrigger="PropertyChanged" />
                </sapv:ExpressionTextBox.Expression>
            </sapv:ExpressionTextBox>

            <Label Grid.Column="1" Content="By" HorizontalAlignment="Center" />
            <Button Content="{Binding Path=ModelItem.Value}" Grid.Column="2" Width="40" x:Name="IncrementButton" Click="IncrementButton_Click" Template="{DynamicResource DefaultButtonStyle}" ToolTip="{x:Static ds:Resources.IncrementOrDecrement}" />
        </Grid>
    </uip:ActivityDecoratorControl>
</sap:ActivityDesigner>